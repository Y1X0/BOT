# Voice-chat music bot stack.
# NOTE: these versions are pinned to a known-working combo. If pyrogram gives
# auth/method errors, swap `pyrogram` + `tgcrypto` for `pyrofork` + `tgcrypto`
# (pyrofork installs under the same `pyrogram` import name).
py-tgcalls==2.1.6
pyrogram==2.0.106
tgcrypto==1.2.5
yt-dlp>=2024.8.6
python-dotenv==1.0.1
